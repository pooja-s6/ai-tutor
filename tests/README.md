# Tests folder for AI Tutor Backend

This folder contains test cases for the backend API.

## Running Tests

Install pytest:
```bash
pip install pytest
```

Run all tests:
```bash
pytest
```

Run specific test file:
```bash
pytest tests/test_chat.py
```

Run with verbose output:
```bash
pytest -v
```
